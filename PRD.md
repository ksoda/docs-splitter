# PRD: 目次に基づくPDF分割

## 1. 目的

PDFの目次構造を基準に学習したいまとまりを選び、元の紙面を保った
分割PDFとして取り出す。利用者は開発者本人だけとする。

初回は雑誌の特集全体を一つのPDFにし、Gemini Notebookへ投入して
特集を通して理解する用途を優先する。複数の雑誌・書籍から同じ
テーマを集める用途もあるが、優先度は低い。

## 2. 範囲

### 対象

- PDFアウトラインに目次がある日本語の書籍・雑誌
- 目次階層で選んだ単位ごとのPDF出力
- 分割境界を人が確認する手順
- 分割定義だけを出すモード（望ましい機能）

### 対象外

- テキストのトークンチャンク化、要約、TTS、ベクトル検索
- OCR、目次の自動認識、PDF内容の自動修正
- UI、クラウドAPI、実装・実行時のLLM呼び出し
- PDFの再配布

目次が使えないPDFを自動で補う機能は初回の対象外とし、停止して報告する。

## 3. 利用シナリオ

1. 利用者がPDFと目次階層を指定する。
2. システムが全ページを分割単位または未分類に割り当て、境界と範囲を示す。
3. 利用者が不一致の境界、未分類に隣接する境界、各境界型からの標本を確認する。
4. 利用者が確認後に実行し、分割PDFを新規出力する。
5. 利用者が個人利用し、生成したPDFを再配布しない。

雑誌は特集単位を優先する。章・記事単位で分ける場合、扉ページを直後の
章・記事に含める。特集単位では特集の扉と配下の章を一つにまとめる。
前付け・後付けはそれぞれ一つの単位にする。書籍の既定単位は未決で、
文書ごとの自動既定判定は初回対象外とする。

## 4. 機能要件

### FR-1: PDFと目次

- PDFのページとアウトラインを読み取る。
- アウトライン階層を保ち、利用者が分割する階層を選べる。
- 目次またはページ範囲に曖昧さ・不整合があれば、推測して出力せず報告する。

### FR-2: ページ範囲

- 全ページを連続した範囲として分割単位または未分類に割り当てる。
- 範囲のページ数と順序を検査する。
- 特集・部・章など選んだ単位の境界を一覧できる。

### FR-3: 境界確認

- 全境界を自動照合する。
- 目次タイトルと本文側の照合文字列は、空白文字を除いた完全一致だけを自動一致とする。
- あいまい一致は候補として示し、人が確認する。
- 不一致と未分類に隣接する境界はすべて人が確認する。
- 一致境界は型ごとの層から各2件確認する。誤りがあった層は全件確認する。
- 1境界あたり数十秒で判断できる表示にする。タイトル、開始ページと直前
  ページの縮小画像、照合結果を示す。

### FR-4: 分割出力

- 確認済みの範囲ごとに、元のページの見た目を保ったPDFを出力する。
- 元PDFを変更せず、既存ファイルを上書きしない。
- 定義のみの出力ではPDFを生成しない。

## 5. 非機能要件

- 実装・実行時にLLMや外部APIを呼び出さない。
- 依存は寛容なライセンスのOSSに限る。PDF処理はpypdfを候補とし、
  PyMuPDFは使わない。追加依存は決定者の確認を得る。
- 合成PDFの再現可能なテストをコミットする。
- 実物PDFの検査はローカル専用とし、環境変数で場所を指定する。
  指定がなければスキップする。元資料の内容を記録せず、結果の要約だけを残す。
- 変更は1コミット1論理変更とし、実装完了と仮説検証完了を分けて記録する。

## 6. 成功条件と停止

初回の成功条件:

- 自動検査で全ページが分割単位または未分類に割り当てられ、ページ数と順序が整合する。
- 規定された境界の人手確認が完了し、不一致が残らない。
- 境界判断が概ね数十秒ででき、確認件数が1冊10〜20件程度に収まる。

境界判断がたびたび数十秒を超えるか、確認件数が見込みを大きく超えた場合は
作業を止めて分割単位と確認方法を見直す。無人実行は1回60分または
Claudeの利用枠到達の早い方で停止する。

自動の正答割合、未分類ページの内容、所要時間は観察値として記録し、
単独の合否判定には使わない。初回の結果を見てしきい値を見直す。

## 7. 初回試行の進め方

初回課題は、既存のテキストチャンクCLIをPDFの目次単位分割へ置き換える。
まず合成PDFによる自動テストを整え、必要な人手境界確認を用意する。
実物PDFでの確認は別途、環境変数が設定された場合のみ実行する。
